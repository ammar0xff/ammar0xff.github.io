# ammar0xff.github.io

Personal profile site.

Built as a static single page with no build step: `index.html`, `styles.css` and one SVG favicon.

To edit: search the file for `EDIT:` comments in `index.html`.
